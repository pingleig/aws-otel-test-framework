#- validationType: "ecs-container-insight-prometheus-metrics"
#  shouldValidateMetricValue: false
#  expectedMetricTemplate: "ECS_CONTAINER_INSIGHT_PROMETHEUS_METRIC"
- validationType: "container-insight-ecs-prometheus-logs"
  shouldValidateMetricValue: false
  # NOTE: the value here matches enum ExpectedLogStructure under fileconfigs pacakge
  expectedLogStructureTemplate: "CONTAINER_INSIGHT_ECS_PROMETHEUS_LOG"
